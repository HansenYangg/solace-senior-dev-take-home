# Example environment variables for Solace Take-Home
# Do NOT include secrets in this file

# --- Task A: AWS Lambda / Decryption Service ---
AWS_REGION=us-east-1
S3_BUCKET=your-bucket-name
KMS_KEY_ID=alias/solace/decrypt
API_URL=https://your-api-gateway-url

# --- Task B: Client SDK (OPTIONAL, works fine without .env) ---
REACT_APP_API_URL=https://your-api-gateway-url

# --- Task C: ASR, TTS, etc. (React frontend) ---
REACT_APP_OPENAI_API_KEY=your-openai-api-key

# --- Polly Proxy (Node backend for TTS) ---
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_REGION=us-east-1
# PORT=5000
